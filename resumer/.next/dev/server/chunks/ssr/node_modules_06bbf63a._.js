module.exports = [
"[project]/node_modules/clsx/dist/clsx.mjs [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "clsx",
    ()=>clsx,
    "default",
    ()=>__TURBOPACK__default__export__
]);
function r(e) {
    var t, f, n = "";
    if ("string" == typeof e || "number" == typeof e) n += e;
    else if ("object" == typeof e) if (Array.isArray(e)) {
        var o = e.length;
        for(t = 0; t < o; t++)e[t] && (f = r(e[t])) && (n && (n += " "), n += f);
    } else for(f in e)e[f] && (n && (n += " "), n += f);
    return n;
}
function clsx() {
    for(var e, t, f = 0, n = "", o = arguments.length; f < o; f++)(e = arguments[f]) && (t = r(e)) && (n && (n += " "), n += t);
    return n;
}
const __TURBOPACK__default__export__ = clsx;
}),
"[project]/node_modules/@swc/helpers/cjs/_interop_require_wildcard.cjs [app-rsc] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) return obj;
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") return {
        default: obj
    };
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) return cache.get(obj);
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) Object.defineProperty(newObj, key, desc);
            else newObj[key] = obj[key];
        }
    }
    newObj.default = obj;
    if (cache) cache.set(obj, newObj);
    return newObj;
}
exports._ = _interop_require_wildcard;
}),
"[project]/node_modules/@kinde/jwt-decoder/dist/jwt-decoder.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "TokenPart",
    ()=>s,
    "jwtDecoder",
    ()=>c
]);
var s = /* @__PURE__ */ ((e)=>(e[e.header = 0] = "header", e[e.body = 1] = "body", e))(s || {});
function c(e, t) {
    if (!e) return null;
    const r = e.split(".");
    if (r.length !== 3) return null;
    const n = r[t ?? 1].replace(/-/g, "+").replace(/_/g, "/"), o = decodeURIComponent(atob(n).split("").map((l)=>"%" + ("00" + l.charCodeAt(0).toString(16)).slice(-2)).join(""));
    return JSON.parse(o);
}
;
}),
"[project]/node_modules/@kinde-oss/kinde-auth-nextjs/dist/node_modules/.pnpm/@kinde-oss_kinde-auth-react@5.8.0_react-dom@19.2.0_react@19.2.0__react@19.2.0/node_modules/@kinde-oss/kinde-auth-react/dist/utils.es.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "IssuerRouteTypes",
    ()=>L,
    "LocalStorage",
    ()=>Se,
    "MemoryStorage",
    ()=>_e,
    "PortalPage",
    ()=>H,
    "PromptTypes",
    ()=>K,
    "RefreshType",
    ()=>y,
    "SessionBase",
    ()=>G,
    "StorageKeys",
    ()=>c,
    "base64UrlEncode",
    ()=>F,
    "checkAuth",
    ()=>Te,
    "clearActiveStorage",
    ()=>Ue,
    "clearRefreshTimer",
    ()=>O,
    "exchangeAuthCode",
    ()=>ke,
    "frameworkSettings",
    ()=>I,
    "generateAuthUrl",
    ()=>ye,
    "generateKindeSDKHeader",
    ()=>le,
    "generatePortalUrl",
    ()=>Me,
    "generateRandomString",
    ()=>M,
    "getActiveStorage",
    ()=>T,
    "getClaim",
    ()=>A,
    "getClaims",
    ()=>Q,
    "getCurrentOrganization",
    ()=>Pe,
    "getDecodedToken",
    ()=>_,
    "getEntitlements",
    ()=>$e,
    "getFlag",
    ()=>xe,
    "getInsecureStorage",
    ()=>$,
    "getPermission",
    ()=>Ee,
    "getPermissions",
    ()=>ve,
    "getRoles",
    ()=>Ie,
    "getUserOrganizations",
    ()=>Re,
    "getUserProfile",
    ()=>be,
    "isAuthenticated",
    ()=>Ae,
    "isClient",
    ()=>R,
    "isCustomDomain",
    ()=>E,
    "isTokenExpired",
    ()=>X,
    "mapLoginMethodParamsForUrl",
    ()=>ie,
    "navigateToKinde",
    ()=>Ne,
    "refreshToken",
    ()=>x,
    "sanitizeUrl",
    ()=>j,
    "sessionManagerActivityProxy",
    ()=>J,
    "setActiveStorage",
    ()=>Ce,
    "setInsecureStorage",
    ()=>Le,
    "setRefreshTimer",
    ()=>q,
    "splitString",
    ()=>B,
    "storageSettings",
    ()=>i,
    "updateActivityTimestamp",
    ()=>fe
]);
var re = Object.defineProperty;
var ne = (e, r, t)=>r in e ? re(e, r, {
        enumerable: !0,
        configurable: !0,
        writable: !0,
        value: t
    }) : e[r] = t;
var P = (e, r, t)=>ne(e, typeof r != "symbol" ? r + "" : r, t);
var K = /* @__PURE__ */ ((e)=>(e.none = "none", e.create = "create", e.login = "login", e))(K || {}), H = /* @__PURE__ */ ((e)=>(e.organizationDetails = "organization_details", e.organizationMembers = "organization_members", e.organizationPlanDetails = "organization_plan_details", e.organizationPaymentDetails = "organization_payment_details", e.organizationPlanSelection = "organization_plan_selection", e.paymentDetails = "payment_details", e.planSelection = "plan_selection", e.planDetails = "plan_details", e.profile = "profile", e))(H || {}), L = /* @__PURE__ */ ((e)=>(e.logout = "logout", e.login = "login", e.register = "registration", e.token = "token", e.profile = "profile", e))(L || {}), y = /* @__PURE__ */ ((e)=>(e[e.refreshToken = 0] = "refreshToken", e[e.cookie = 1] = "cookie", e))(y || {});
const F = (e)=>{
    const r = (o)=>btoa(o).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
    if (e instanceof ArrayBuffer) {
        const o = new Uint8Array(e), s = String.fromCharCode(...o);
        return r(s);
    }
    const t = new TextEncoder().encode(e), n = String.fromCharCode(...t);
    return r(n);
}, M = (e = 28)=>{
    if (crypto) {
        const r = new Uint8Array(e / 2);
        return crypto.getRandomValues(r), Array.from(r, oe).join("");
    } else return se(e);
};
function oe(e) {
    return e.toString(16).padStart(2, "0");
}
function se(e = 28) {
    const r = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    let t = "";
    const n = r.length;
    for(let o = 0; o < e; o++)t += r.charAt(Math.floor(Math.random() * n));
    return t;
}
const j = (e)=>{
    let r = e.replace(/([^:]\/)\/+/g, "$1");
    return r.match(/^\/\/[^/?]+/) && r.includes(".") ? r = r.replace(/^\/\/\/+/, "//") : r.match(/^\/\/+/) && (r = r.replace(/^\/\/+/, "/")), r !== "/" && (r = r.replace(/\/$/, "")), r;
}, ie = (e, r = !1)=>{
    var o, s, l, d;
    const t = Array.isArray(e.audience) ? e.audience.join(" ") : e.audience || "", n = {
        login_hint: e.loginHint,
        is_create_org: (o = e.isCreateOrg) == null ? void 0 : o.toString(),
        connection_id: e.connectionId,
        redirect_uri: e.redirectURL ? r ? e.redirectURL : j(e.redirectURL) : void 0,
        audience: t,
        scope: ((s = e.scope) == null ? void 0 : s.join(" ")) || "email profile openid offline",
        prompt: e.prompt,
        lang: e.lang,
        org_code: e.orgCode,
        org_name: e.orgName,
        has_success_page: (l = e.hasSuccessPage) == null ? void 0 : l.toString(),
        workflow_deployment_id: e.workflowDeploymentId,
        supports_reauth: (d = e.supportsReauth) == null ? void 0 : d.toString(),
        plan_interest: e.planInterest,
        pricing_table_key: e.pricingTableKey,
        pages_mode: e.pagesMode
    };
    return Object.keys(n).forEach((u)=>n[u] === void 0 && delete n[u]), n;
}, N = (e)=>typeof e != "object" || e === null ? e : Array.isArray(e) ? e.map((r)=>N(r)) : Object.fromEntries(Object.entries(e).map(([r, t])=>[
            r.replace(/_([a-z])/g, (n, o)=>o.toUpperCase()),
            N(t)
        ])), ae = [
    // UTM tags
    "utm_source",
    "utm_medium",
    "utm_campaign",
    "utm_content",
    "utm_term",
    // Google Ads smart campaign tracking
    "gclid",
    "click_id",
    "hsa_acc",
    "hsa_cam",
    "hsa_grp",
    "hsa_ad",
    "hsa_src",
    "hsa_tgt",
    "hsa_kw",
    "hsa_mt",
    "hsa_net",
    "hsa_ver",
    // Marketing category
    "match_type",
    "keyword",
    "device",
    "ad_group_id",
    "campaign_id",
    "creative",
    "network",
    "ad_position",
    "fbclid",
    "li_fat_id",
    "msclkid",
    "twclid",
    "ttclid"
], ye = async (e, r = L.login, t, n)=>{
    const o = `${e}/oauth2/auth`, s = $();
    if (t.reauthState) try {
        const a = N(JSON.parse(atob(t.reauthState)));
        t = {
            ...t,
            ...a
        }, delete t.reauthState;
    } catch (a) {
        const h = a instanceof Error ? a.message : "Unknown error";
        throw new Error(`Error handing reauth state: ${h}`);
    }
    if (!t.clientId) throw new Error("Error generating auth URL: Client ID missing");
    const l = {
        client_id: t.clientId,
        response_type: t.responseType || "code",
        ...ie(t, n == null ? void 0 : n.disableUrlSanitization)
    };
    t.state || (t.state = M(32)), s && s.setSessionItem(c.state, t.state), l.state = t.state, t.nonce || (t.nonce = M(16)), l.nonce = t.nonce, s && s.setSessionItem(c.nonce, t.nonce);
    let d = "";
    if (t.codeChallenge) l.code_challenge = t.codeChallenge;
    else {
        const { codeVerifier: a, codeChallenge: h } = await ce();
        d = a, s && s.setSessionItem(c.codeVerifier, a), l.code_challenge = h;
    }
    l.code_challenge_method = "S256", t.codeChallengeMethod && (l.code_challenge_method = t.codeChallengeMethod), !t.prompt && r === L.register && (l.prompt = K.create), t.properties && Object.keys(t.properties).forEach((a)=>{
        var f;
        if (!ae.includes(a)) {
            console.warn("Unsupported Property for url generation: ", a);
            return;
        }
        const h = (f = t.properties) == null ? void 0 : f[a];
        h !== void 0 && (l[a] = h);
    });
    const u = new URLSearchParams(l).toString();
    return {
        url: new URL(`${o}?${u}`),
        state: l.state,
        nonce: l.nonce,
        codeChallenge: l.code_challenge,
        codeVerifier: d
    };
};
async function ce() {
    const e = M(52), r = new TextEncoder().encode(e);
    let t = "";
    if (!crypto) t = F(btoa(e));
    else {
        const n = await crypto.subtle.digest("SHA-256", r);
        t = F(n);
    }
    return {
        codeVerifier: e,
        codeChallenge: t
    };
}
const R = ()=>("TURBOPACK compile-time value", "undefined") < "u";
let b;
function q(e, r) {
    if (O(), !R()) throw new Error("setRefreshTimer requires a browser environment");
    if (e <= 0) throw new Error("Timer duration must be positive");
    b = window.setTimeout(r, Math.min(e * 1e3 - 1e4, 864e5));
}
function O() {
    R() && b !== void 0 && (window.clearTimeout(b), b = void 0);
}
const I = {
    framework: "",
    frameworkVersion: "",
    sdkVersion: ""
}, W = async ()=>{
    var e;
    await ((e = $()) == null ? void 0 : e.removeItems(c.state, c.nonce, c.codeVerifier));
}, le = ()=>`${I.framework}/${I.sdkVersion}/${I.frameworkVersion}/Javascript`, ke = async ({ urlParams: e, domain: r, clientId: t, clientSecret: n, redirectURL: o, autoRefresh: s = !1, onRefresh: l })=>{
    const d = e.get("state"), u = e.get("code");
    if (!d || !u) return console.error("Invalid state or code"), {
        success: !1,
        error: "Invalid state or code"
    };
    const a = $();
    if (!a) return console.error("No active storage found"), {
        success: !1,
        error: "Authentication storage is not initialized"
    };
    (!I.framework || !I.frameworkVersion) && console.warn("Framework and version not set. Please set the framework and version in the config object");
    const h = await a.getSessionItem(c.state);
    if (d !== h) return console.error("Invalid state"), {
        success: !1,
        error: `Invalid state; supplied ${d}, expected ${h}`
    };
    const f = await a.getSessionItem(c.codeVerifier);
    if (f === null) return console.error("Code verifier not found"), {
        success: !1,
        error: "Code verifier not found"
    };
    const m = {
        "Content-type": "application/x-www-form-urlencoded; charset=UTF-8"
    };
    I.framework && (m["Kinde-SDK"] = le());
    const S = !i.useInsecureForRefreshToken && E(r) ? {
        credentials: "include"
    } : {}, v = new URLSearchParams({
        client_id: t,
        code: u,
        code_verifier: f,
        grant_type: "authorization_code",
        redirect_uri: o
    });
    n && v.append("client_secret", n);
    const ee = {
        method: "POST",
        ...S,
        headers: new Headers(m),
        body: v
    };
    let w;
    O();
    try {
        if (w = await fetch(`${r}/oauth2/token`, ee), !(w != null && w.ok)) {
            const p = await w.text();
            return console.error("Token exchange failed:", w.status, p), {
                success: !1,
                error: `Token exchange failed: ${w.status} - ${p}`
            };
        }
    } catch (p) {
        return W(), console.error("Token exchange failed:", p), {
            success: !1,
            error: `Token exchange failed: ${p}`
        };
    }
    const g = await w.json(), D = T();
    D && D.setItems({
        [c.accessToken]: g.access_token,
        [c.idToken]: g.id_token,
        [c.refreshToken]: g.refresh_token
    }), (i.useInsecureForRefreshToken || !E(r)) && a.setSessionItem(c.refreshToken, g.refresh_token), s && q(g.expires_in, async ()=>{
        x({
            domain: r,
            clientId: t,
            onRefresh: l
        });
    }), W();
    const te = (p)=>(p.search = "", p);
    if (R()) {
        const p = te(new URL(window.location.toString()));
        window.history.replaceState(window.history.state, "", p);
    }
    return !g.access_token || !g.id_token || !g.refresh_token ? {
        success: !1,
        error: "No access token received"
    } : {
        success: !0,
        [c.accessToken]: g.access_token,
        [c.idToken]: g.id_token,
        [c.refreshToken]: g.refresh_token
    };
};
function ue(e) {
    const r = document.cookie.split("; ").find((t)=>t.startsWith(`${e}=`));
    if (!r) return null;
    try {
        const t = r.split("=")[1];
        return t ? decodeURIComponent(t) : null;
    } catch (t) {
        return console.error(`Error parsing cookie ${e}:`, t), null;
    }
}
const de = "_kbrte", Te = async ({ domain: e, clientId: r })=>{
    if (!e) return {
        success: !1,
        error: "Domain is required for authentication check"
    };
    if (!r) return {
        success: !1,
        error: "Client ID is required for authentication check"
    };
    const t = T();
    if (t) {
        const { [c.accessToken]: l, [c.idToken]: d, [c.refreshToken]: u } = await t.getItems(c.accessToken, c.idToken, c.refreshToken);
        if (l && d && u) return await X({
            threshold: 10
        }) ? await x({
            domain: e,
            clientId: r,
            refreshType: y.refreshToken
        }) : {
            success: !0,
            accessToken: l,
            idToken: d,
            refreshToken: u
        };
    }
    const n = E(e), o = i.useInsecureForRefreshToken;
    let s = null;
    return n && !o && (s = ue(de)), await x({
        domain: e,
        clientId: r,
        refreshType: s ? y.cookie : y.refreshToken
    });
}, E = (e)=>!e.match(/^(?:https?:\/\/)?[a-zA-Z0-9][.-a-zA-Z0-9]*\.kinde\.com$/i);
function B(e, r) {
    return r <= 0 ? [] : e.match(new RegExp(`.{1,${r}}`, "g")) || [];
}
var c = /* @__PURE__ */ ((e)=>(e.accessToken = "accessToken", e.idToken = "idToken", e.refreshToken = "refreshToken", e.state = "state", e.nonce = "nonce", e.codeVerifier = "codeVerifier", e))(c || {}), z = /* @__PURE__ */ ((e)=>(e.preWarning = "preWarning", e.timeout = "timeout", e))(z || {});
class G {
    constructor(){
        P(this, "listeners", /* @__PURE__ */ new Set());
        P(this, "notificationScheduled", !1);
    }
    notifyListeners() {
        this.listeners.size !== 0 && this.scheduleNotification();
    }
    async scheduleNotification() {
        this.notificationScheduled || (this.notificationScheduled = !0, await new Promise((r)=>{
            setTimeout(async ()=>{
                await Promise.all(Array.from(this.listeners).map((t)=>t())), this.notificationScheduled = !1, r();
            }, 0);
        }));
    }
    subscribe(r) {
        return this.listeners.add(r), ()=>{
            this.listeners.delete(r);
        };
    }
    async setItems(r) {
        await Promise.all(Object.entries(r).map(([t, n])=>this.setSessionItem(t, n)));
    }
    async getItems(...r) {
        const t = r.map(async (o)=>{
            const s = await this.getSessionItem(o);
            return [
                o,
                s
            ];
        }), n = await Promise.all(t);
        return Object.fromEntries(n);
    }
    async removeItems(...r) {
        await Promise.all(r.map((t)=>this.removeSessionItem(t)));
    }
}
class _e extends G {
    constructor(){
        super(...arguments);
        P(this, "memCache", {});
    }
    /**
   * Clears all items from session store.
   * @returns {void}
   */ async destroySession() {
        this.memCache = {}, this.notifyListeners();
    }
    /**
   * Sets the provided key-value store to the memory cache.
   * @param {string} itemKey
   * @param {unknown} itemValue
   * @returns {void}
   */ async setSessionItem(t, n) {
        if (await this.removeSessionItem(t), typeof n == "string") {
            B(n, i.maxLength).forEach((o, s)=>{
                this.memCache[`${i.keyPrefix}${t}${s}`] = o;
            }), this.notifyListeners();
            return;
        }
        this.memCache[`${i.keyPrefix}${String(t)}0`] = n, this.notifyListeners();
    }
    /**
   * Gets the item for the provided key from the memory cache.
   * @param {string} itemKey
   * @returns {unknown | null}
   */ async getSessionItem(t) {
        if (this.memCache[`${i.keyPrefix}${String(t)}0`] === void 0) return null;
        let n = "", o = 0, s = `${i.keyPrefix}${String(t)}${o}`;
        for(; this.memCache[s] !== void 0;)n += this.memCache[s], o++, s = `${i.keyPrefix}${String(t)}${o}`;
        return n;
    }
    /**
   * Removes the item for the provided key from the memory cache.
   * @param {string} itemKey
   * @returns {void}
   */ async removeSessionItem(t) {
        for(const n in this.memCache)n.startsWith(`${i.keyPrefix}${String(t)}`) && delete this.memCache[n];
        this.notifyListeners();
    }
}
class Se extends G {
    constructor(){
        super();
        P(this, "internalItems", /* @__PURE__ */ new Set());
        i.useInsecureForRefreshToken && console.warn("LocalStorage store should not be used in production");
    }
    /**
   * Clears all items from session store.
   * @returns {void}
   */ async destroySession() {
        await Promise.all(Array.from(this.internalItems).map((t)=>this.removeSessionItem(t))), this.notifyListeners();
    }
    /**
   * Sets the provided key-value store to the localStorage cache.
   * @param {V} itemKey
   * @param {unknown} itemValue
   * @returns {void}
   */ async setSessionItem(t, n) {
        if (await this.removeSessionItem(t), this.internalItems.add(t), typeof n == "string") {
            B(n, i.maxLength).forEach((o, s)=>{
                localStorage.setItem(`${i.keyPrefix}${t}${s}`, o);
            }), this.notifyListeners();
            return;
        }
        localStorage.setItem(`${i.keyPrefix}${t}0`, n), this.notifyListeners();
    }
    /**
   * Gets the item for the provided key from the localStorage cache.
   * @param {string} itemKey
   * @returns {unknown | null}
   */ async getSessionItem(t) {
        if (localStorage.getItem(`${i.keyPrefix}${t}0`) === null) return null;
        let n = "", o = 0, s = `${i.keyPrefix}${String(t)}${o}`;
        for(; localStorage.getItem(s) !== null;)n += localStorage.getItem(s), o++, s = `${i.keyPrefix}${String(t)}${o}`;
        return n;
    }
    /**
   * Removes the item for the provided key from the localStorage cache.
   * @param {V} itemKey
   * @returns {void}
   */ async removeSessionItem(t) {
        let n = 0;
        for(; localStorage.getItem(`${i.keyPrefix}${String(t)}${n}`) !== null;)localStorage.removeItem(`${i.keyPrefix}${String(t)}${n}`), n++;
        this.internalItems.delete(t), this.notifyListeners();
    }
}
const i = {
    /**
   * The prefix to use for the storage keys.
   */ keyPrefix: "kinde-",
    /**
   * The maximum length of the storage.
   *
   * If the length is exceeded the items will be split into multiple storage items.
   */ maxLength: 2e3,
    /**
   * Use insecure storage for refresh token.
   *
   * Warning: This should only be used when you're not using a custom domain and no backend app to authenticate on.
   */ useInsecureForRefreshToken: !1,
    /**
   * The number of minutes of inactivity before tokens are considered expired.
   *
   * When undefined, activity tracking is disabled.
   */ activityTimeoutMinutes: void 0,
    /**
   * The number of minutes of inactivity before a pre-warning is shown.
   *
   * When undefined, pre-warning is disabled.
   */ activityTimeoutPreWarningMinutes: void 0,
    /**
   * The function to call when the activity timeout is reached.
   *
   * @param timeoutType - The type of timeout that occurred.
   */ onActivityTimeout: void 0
};
let C = null, U = null;
const fe = ()=>{
    const e = T() ?? $();
    if (!e) throw new Error("Session manager not found");
    if (!i.activityTimeoutMinutes) throw new Error("No activity timeout minutes set");
    if (i.activityTimeoutPreWarningMinutes !== void 0 && i.activityTimeoutPreWarningMinutes >= i.activityTimeoutMinutes) throw new Error("activityTimeoutPreWarningMinutes must be less than activityTimeoutMinutes");
    C && clearTimeout(C), U && clearTimeout(U), U = setTimeout(async ()=>{
        var t;
        try {
            await e.destroySession();
        } catch (n) {
            console.error("Failed to destroy secure session:", n);
        }
        const r = $();
        if (r && r !== e) try {
            await r.destroySession();
        } catch (n) {
            console.error("Failed to destroy insecure session:", n);
        }
        try {
            (t = i.onActivityTimeout) == null || t.call(i, z.timeout);
        } catch (n) {
            console.error("[activityTimeout] onActivityTimeout(timeout) threw:", n);
        }
    }, i.activityTimeoutMinutes * 60 * 1e3), i.activityTimeoutPreWarningMinutes !== void 0 && (C = setTimeout(()=>{
        var r;
        try {
            (r = i.onActivityTimeout) == null || r.call(i, z.preWarning);
        } catch (t) {
            console.error("[activityTimeout] onActivityTimeout(preWarning) threw:", t);
        }
    }, i.activityTimeoutPreWarningMinutes * 60 * 1e3));
}, J = (e)=>{
    if (!e) throw new Error("Session manager not found");
    if (!i.activityTimeoutMinutes) return e;
    if (i.activityTimeoutPreWarningMinutes !== void 0 && i.activityTimeoutPreWarningMinutes >= i.activityTimeoutMinutes) throw new Error("activityTimeoutPreWarningMinutes must be less than activityTimeoutMinutes");
    const r = {
        get (t, n) {
            const o = t[n];
            return typeof o == "function" ? (n !== "destroySession" && fe(), o.bind(t)) : o;
        }
    };
    return new Proxy(e, r);
}, Z = (e, r)=>{
    if (Array.isArray(e) && Array.isArray(r)) return Array.from(/* @__PURE__ */ new Set([
        ...e,
        ...r
    ]));
    if (e && typeof e == "object" && r && typeof r == "object") {
        const t = {
            ...e
        };
        for (const n of Object.keys(r))n in t ? t[n] = Z(t[n], r[n]) : t[n] = r[n];
        return t;
    }
    return r;
};
async function V(e) {
    const r = T();
    if (!r) throw new Error("No active storage found.");
    const t = await r.getSessionItem(c.accessToken);
    if (!t) throw new Error("Authentication token not found.");
    const n = await A("iss");
    if (!(n != null && n.value)) throw new Error("Domain (iss claim) not found.");
    let o;
    try {
        o = await fetch(`${n.value}/${e}`, {
            method: "GET",
            headers: {
                Authorization: `Bearer ${t}`,
                "Content-Type": "application/json"
            }
        });
    } catch (s) {
        throw new Error(`Failed to fetch from ${n.value}/${e}: ${s}`);
    }
    if (!o.ok) throw new Error(`API request failed with status ${o.status}`);
    return await o.json();
}
const Y = async ({ url: e })=>{
    var n;
    let r = [], t = await V(e);
    if (r = t.data, (n = t.metadata) == null ? void 0 : n.has_more) {
        let o = t.metadata.next_page_starting_after;
        for(; t.metadata.has_more;)t = await V(`${e}?starting_after=${o}`), r = Z(r, t.data), o = t.metadata.next_page_starting_after;
    }
    return r;
}, ve = async (e)=>{
    const r = await _();
    if (!r) return {
        orgCode: null,
        permissions: []
    };
    const t = r.permissions || r["x-hasura-permissions"] || [];
    return {
        orgCode: r.org_code || r["x-hasura-org-code"],
        permissions: t
    };
}, Ie = async (e)=>{
    var n;
    const r = await A("roles");
    if (!(r != null && r.value)) return ((n = (await Y({
        url: "account_api/v1/roles"
    })).roles) == null ? void 0 : n.map((o)=>({
            id: o.id,
            name: o.name,
            key: o.key
        }))) || [];
    const t = await _();
    return t ? !t.roles && !t["x-hasura-roles"] ? (console.warn("No roles found in token, ensure roles have been included in the token customisation within the application settings"), []) : t.roles || t["x-hasura-roles"] : [];
}, $e = async ()=>{
    var r, t;
    const e = await Y({
        url: "account_api/v1/entitlements"
    });
    return {
        orgCode: e.org_code,
        plans: ((r = e.plans) == null ? void 0 : r.map((n)=>({
                key: n.key,
                subscribedOn: n.subscribed_on
            }))) || [],
        entitlements: ((t = e.entitlements) == null ? void 0 : t.map((n)=>({
                id: n.id,
                fixedCharge: n.fixed_charge,
                priceName: n.price_name,
                unitAmount: n.unit_amount,
                featureKey: n.feature_key,
                featureName: n.feature_name,
                entitlementLimitMax: n.entitlement_limit_max,
                entitlementLimitMin: n.entitlement_limit_min
            }))) || []
    };
};
function he(e, r) {
    if (!e) return null;
    const t = e.split(".");
    if (t.length !== 3) return null;
    const n = t[1].replace(/-/g, "+").replace(/_/g, "/"), o = decodeURIComponent(atob(n).split("").map((s)=>"%" + ("00" + s.charCodeAt(0).toString(16)).slice(-2)).join(""));
    return JSON.parse(o);
}
const _ = async (e = c.accessToken)=>{
    const r = T();
    if (!r) return null;
    const t = await r.getSessionItem(e === "accessToken" ? c.accessToken : c.idToken);
    if (!t) return null;
    const n = he(t);
    return n || console.warn("No decoded token found"), n;
}, Q = async (e = "accessToken")=>_(e), A = async (e, r = "accessToken")=>{
    const t = await Q(r);
    return t ? {
        name: e,
        value: t[e]
    } : null;
}, Pe = async ()=>{
    const e = await _();
    return e ? e.org_code || e["x-hasura-org-code"] : null;
}, xe = async (e, r)=>{
    var o;
    const t = await _();
    if (!t) return null;
    const n = t.feature_flags || t["x-hasura-feature-flags"];
    return n ? ((o = n[e]) == null ? void 0 : o.v) ?? null : null;
}, be = async ()=>{
    const e = await Q("idToken");
    if (!e) return null;
    const { sub: r } = e;
    return r ? {
        id: e.sub,
        givenName: e.given_name,
        familyName: e.family_name,
        email: e.email,
        picture: e.picture
    } : (console.error("No sub in idToken"), null);
}, Ee = async (e, r)=>{
    const t = await _();
    if (!t) return {
        permissionKey: e,
        orgCode: null,
        isGranted: !1
    };
    const n = t.permissions || [];
    return {
        permissionKey: e,
        orgCode: t.org_code,
        isGranted: !!n.includes(e)
    };
}, Re = async ()=>{
    const e = await _("idToken");
    return e ? !e.org_codes && !e["x-hasura-org-codes"] ? (console.warn("Org codes not found in token, ensure org codes have been included in the token customisation within the application settings"), null) : e.org_codes || e["x-hasura-org-codes"] : null;
}, Ae = async (e)=>{
    try {
        const r = await X({
            threshold: e == null ? void 0 : e.expiredThreshold
        });
        return r && e != null && e.useRefreshToken ? (await x({
            domain: e.domain,
            clientId: e.clientId
        })).success : !r;
    } catch (r) {
        return console.error("Error checking authentication:", r), !1;
    }
}, X = async (e)=>{
    try {
        const r = await _("accessToken");
        return r ? r.exp ? r.exp < Math.floor(Date.now() / 1e3) + ((e == null ? void 0 : e.threshold) || 0) : (console.error("Token does not have an expiry"), !0) : !0;
    } catch (r) {
        return console.error("Error checking authentication:", r), !1;
    }
}, x = async ({ domain: e, clientId: r, clientSecret: t, refreshType: n = y.refreshToken, onRefresh: o })=>{
    var h;
    const s = (f)=>(o && o(f), f);
    if (!e) return s({
        success: !1,
        error: "Domain is required for token refresh"
    });
    if (!r) return s({
        success: !1,
        error: "Client ID is required for token refresh"
    });
    let l = "", d;
    if (i.useInsecureForRefreshToken || !E(e) ? d = $() : d = T(), n === y.refreshToken) {
        if (!d) return s({
            success: !1,
            error: "No active storage found"
        });
        if (l = await d.getSessionItem(c.refreshToken), !l) return s({
            success: !1,
            error: "No refresh token found"
        });
    }
    O();
    let u, a;
    try {
        if (i.onRefreshHandler) u = await i.onRefreshHandler(n);
        else {
            const f = new URLSearchParams({
                ...n === y.refreshToken && {
                    refresh_token: l
                },
                grant_type: "refresh_token",
                client_id: r
            });
            t && f.append("client_secret", t);
            const m = await fetch(`${j(e)}/oauth2/token`, {
                method: "POST",
                ...n === y.cookie && {
                    credentials: "include"
                },
                headers: {
                    "Content-type": "application/x-www-form-urlencoded; charset=UTF-8"
                },
                body: f.toString()
            });
            if (!m.ok) return s({
                success: !1,
                error: "Failed to refresh token"
            });
            a = await m.json(), u = {
                success: !0,
                [c.accessToken]: a.access_token,
                [c.idToken]: a.id_token,
                [c.refreshToken]: a.refresh_token
            };
        }
        if (u.accessToken) {
            const f = T();
            if (!f) return s({
                success: !1,
                error: "No active storage found"
            });
            if (await f.setSessionItem(c.accessToken, u.accessToken), u.idToken && await f.setSessionItem(c.idToken, u.idToken), d && u.refreshToken && await d.setSessionItem(c.refreshToken, u.refreshToken), R()) {
                const m = Number((h = await A("exp")) == null ? void 0 : h.value);
                if (Number.isFinite(m) || a != null && a.expires_in) {
                    let S = 0;
                    if (!(a != null && a.expires_in)) {
                        const v = Math.floor(Date.now() / 1e3);
                        S = Math.max(m - v, 1);
                    }
                    q((a == null ? void 0 : a.expires_in) || S, async ()=>{
                        x({
                            domain: e,
                            clientId: r,
                            refreshType: n,
                            onRefresh: o
                        });
                    });
                }
            }
            return s(u);
        }
    } catch (f) {
        return s({
            success: !1,
            error: `No access token received: ${f}`
        });
    }
    return s({
        success: !1,
        error: "No access token received"
    });
}, k = {
    secure: null,
    insecure: null
}, Ce = (e)=>{
    if (i.activityTimeoutMinutes) {
        k.secure = J(e);
        return;
    }
    k.secure = e;
}, T = ()=>k.secure || null, Ue = ()=>{
    k.secure = null;
}, Le = (e)=>{
    if (i.activityTimeoutMinutes) {
        k.insecure = J(e);
        return;
    }
    k.insecure = e;
}, $ = ()=>k.insecure || k.secure || null;
function me(e, r = []) {
    try {
        const t = new URL(e);
        return !r.includes(t.protocol) && !!t.host;
    } catch  {
        return !1;
    }
}
const Me = async ({ domain: e, returnUrl: r, subNav: t })=>{
    const n = T();
    if (!n) throw new Error("generatePortalUrl: Active storage not found");
    const o = await n.getSessionItem(c.accessToken);
    if (!o) throw new Error("generatePortalUrl: Access Token not found");
    if (!e || typeof e != "string") {
        const u = await A("iss");
        if (!(u != null && u.value) || typeof u.value != "string") throw new Error("generatePortalUrl: Unable to determine domain from access token");
        e = u.value;
    }
    if (!me(r, [
        "ftp:",
        "ws:"
    ])) throw new Error("generatePortalUrl: returnUrl must be an absolute URL");
    const s = new URLSearchParams({
        subnav: t || H.profile,
        return_url: r
    }), l = await fetch(`${j(e)}/account_api/v1/portal_link?${s.toString()}`, {
        headers: {
            Authorization: `Bearer ${o}`
        }
    });
    if (!l.ok) throw new Error(`Failed to fetch profile URL: ${l.status} ${l.statusText}`);
    const d = await l.json();
    if (!d.url || typeof d.url != "string") throw new Error("Invalid URL received from API");
    try {
        return {
            url: new URL(d.url)
        };
    } catch (u) {
        throw console.error(u), new Error(`Invalid URL format received from API: ${d.url}`);
    }
}, ge = ()=>window.self !== window.top, Ne = async ({ url: e, popupOptions: r = {}, handleResult: t, forcePopup: n = !1 })=>{
    ge() || n ? await pe({
        url: e,
        popupOptions: r,
        handleResult: t
    }) : document.location = e;
}, pe = async ({ url: e, popupOptions: r = {}, handleResult: t })=>{
    const { width: n = 500, height: o = 600, left: s = (window.screen.width - n) / 2, top: l = (window.screen.height - o) / 2 } = r, d = window.open(e, "kinde_auth_popup", `width=${n},height=${o},left=${s},top=${l},scrollbars=yes,resizable=yes,toolbar=no,menubar=no,location=no,status=no`);
    if (!d) throw new Error("Popup was blocked by the browser");
    const u = ()=>new Promise((a)=>{
            const h = (f)=>{
                if (f.origin === window.location.origin && f.data && f.data.type === "KINDE_AUTH_RESULT") {
                    window.removeEventListener("message", h);
                    const m = new URLSearchParams();
                    Object.entries(f.data.result).forEach(([S, v])=>{
                        m.append(S, v);
                    }), t == null || t(m).then(()=>a());
                }
            };
            window.addEventListener("message", h);
        });
    try {
        await u();
    } catch (a) {
        throw new Error("Popup authentication failed: " + a);
    }
    return d;
};
;
}),
"[project]/node_modules/destr/dist/index.mjs [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>destr,
    "destr",
    ()=>destr,
    "safeDestr",
    ()=>safeDestr
]);
const suspectProtoRx = /"(?:_|\\u0{2}5[Ff]){2}(?:p|\\u0{2}70)(?:r|\\u0{2}72)(?:o|\\u0{2}6[Ff])(?:t|\\u0{2}74)(?:o|\\u0{2}6[Ff])(?:_|\\u0{2}5[Ff]){2}"\s*:/;
const suspectConstructorRx = /"(?:c|\\u0063)(?:o|\\u006[Ff])(?:n|\\u006[Ee])(?:s|\\u0073)(?:t|\\u0074)(?:r|\\u0072)(?:u|\\u0075)(?:c|\\u0063)(?:t|\\u0074)(?:o|\\u006[Ff])(?:r|\\u0072)"\s*:/;
const JsonSigRx = /^\s*["[{]|^\s*-?\d{1,16}(\.\d{1,17})?([Ee][+-]?\d+)?\s*$/;
function jsonParseTransform(key, value) {
    if (key === "__proto__" || key === "constructor" && value && typeof value === "object" && "prototype" in value) {
        warnKeyDropped(key);
        return;
    }
    return value;
}
function warnKeyDropped(key) {
    console.warn(`[destr] Dropping "${key}" key to prevent prototype pollution.`);
}
function destr(value, options = {}) {
    if (typeof value !== "string") {
        return value;
    }
    if (value[0] === '"' && value[value.length - 1] === '"' && value.indexOf("\\") === -1) {
        return value.slice(1, -1);
    }
    const _value = value.trim();
    if (_value.length <= 9) {
        switch(_value.toLowerCase()){
            case "true":
                {
                    return true;
                }
            case "false":
                {
                    return false;
                }
            case "undefined":
                {
                    return void 0;
                }
            case "null":
                {
                    return null;
                }
            case "nan":
                {
                    return Number.NaN;
                }
            case "infinity":
                {
                    return Number.POSITIVE_INFINITY;
                }
            case "-infinity":
                {
                    return Number.NEGATIVE_INFINITY;
                }
        }
    }
    if (!JsonSigRx.test(value)) {
        if (options.strict) {
            throw new SyntaxError("[destr] Invalid JSON");
        }
        return value;
    }
    try {
        if (suspectProtoRx.test(value) || suspectConstructorRx.test(value)) {
            if (options.strict) {
                throw new Error("[destr] Possible prototype pollution");
            }
            return JSON.parse(value, jsonParseTransform);
        }
        return JSON.parse(value);
    } catch (error) {
        if (options.strict) {
            throw error;
        }
        return value;
    }
}
function safeDestr(value, options = {}) {
    return destr(value, {
        ...options,
        strict: true
    });
}
;
}),
"[project]/node_modules/@kinde-oss/kinde-auth-nextjs/node_modules/cookie/dist/index.js [app-rsc] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.parse = parse;
exports.serialize = serialize;
/**
 * RegExp to match cookie-name in RFC 6265 sec 4.1.1
 * This refers out to the obsoleted definition of token in RFC 2616 sec 2.2
 * which has been replaced by the token definition in RFC 7230 appendix B.
 *
 * cookie-name       = token
 * token             = 1*tchar
 * tchar             = "!" / "#" / "$" / "%" / "&" / "'" /
 *                     "*" / "+" / "-" / "." / "^" / "_" /
 *                     "`" / "|" / "~" / DIGIT / ALPHA
 *
 * Note: Allowing more characters - https://github.com/jshttp/cookie/issues/191
 * Allow same range as cookie value, except `=`, which delimits end of name.
 */ const cookieNameRegExp = /^[\u0021-\u003A\u003C\u003E-\u007E]+$/;
/**
 * RegExp to match cookie-value in RFC 6265 sec 4.1.1
 *
 * cookie-value      = *cookie-octet / ( DQUOTE *cookie-octet DQUOTE )
 * cookie-octet      = %x21 / %x23-2B / %x2D-3A / %x3C-5B / %x5D-7E
 *                     ; US-ASCII characters excluding CTLs,
 *                     ; whitespace DQUOTE, comma, semicolon,
 *                     ; and backslash
 *
 * Allowing more characters: https://github.com/jshttp/cookie/issues/191
 * Comma, backslash, and DQUOTE are not part of the parsing algorithm.
 */ const cookieValueRegExp = /^[\u0021-\u003A\u003C-\u007E]*$/;
/**
 * RegExp to match domain-value in RFC 6265 sec 4.1.1
 *
 * domain-value      = <subdomain>
 *                     ; defined in [RFC1034], Section 3.5, as
 *                     ; enhanced by [RFC1123], Section 2.1
 * <subdomain>       = <label> | <subdomain> "." <label>
 * <label>           = <let-dig> [ [ <ldh-str> ] <let-dig> ]
 *                     Labels must be 63 characters or less.
 *                     'let-dig' not 'letter' in the first char, per RFC1123
 * <ldh-str>         = <let-dig-hyp> | <let-dig-hyp> <ldh-str>
 * <let-dig-hyp>     = <let-dig> | "-"
 * <let-dig>         = <letter> | <digit>
 * <letter>          = any one of the 52 alphabetic characters A through Z in
 *                     upper case and a through z in lower case
 * <digit>           = any one of the ten digits 0 through 9
 *
 * Keep support for leading dot: https://github.com/jshttp/cookie/issues/173
 *
 * > (Note that a leading %x2E ("."), if present, is ignored even though that
 * character is not permitted, but a trailing %x2E ("."), if present, will
 * cause the user agent to ignore the attribute.)
 */ const domainValueRegExp = /^([.]?[a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)([.][a-z0-9]([a-z0-9-]{0,61}[a-z0-9])?)*$/i;
/**
 * RegExp to match path-value in RFC 6265 sec 4.1.1
 *
 * path-value        = <any CHAR except CTLs or ";">
 * CHAR              = %x01-7F
 *                     ; defined in RFC 5234 appendix B.1
 */ const pathValueRegExp = /^[\u0020-\u003A\u003D-\u007E]*$/;
const __toString = Object.prototype.toString;
const NullObject = /* @__PURE__ */ (()=>{
    const C = function() {};
    C.prototype = Object.create(null);
    return C;
})();
/**
 * Parse a cookie header.
 *
 * Parse the given cookie header string into an object
 * The object has the various cookies as keys(names) => values
 */ function parse(str, options) {
    const obj = new NullObject();
    const len = str.length;
    // RFC 6265 sec 4.1.1, RFC 2616 2.2 defines a cookie name consists of one char minimum, plus '='.
    if (len < 2) return obj;
    const dec = options?.decode || decode;
    let index = 0;
    do {
        const eqIdx = str.indexOf("=", index);
        if (eqIdx === -1) break; // No more cookie pairs.
        const colonIdx = str.indexOf(";", index);
        const endIdx = colonIdx === -1 ? len : colonIdx;
        if (eqIdx > endIdx) {
            // backtrack on prior semicolon
            index = str.lastIndexOf(";", eqIdx - 1) + 1;
            continue;
        }
        const keyStartIdx = startIndex(str, index, eqIdx);
        const keyEndIdx = endIndex(str, eqIdx, keyStartIdx);
        const key = str.slice(keyStartIdx, keyEndIdx);
        // only assign once
        if (obj[key] === undefined) {
            let valStartIdx = startIndex(str, eqIdx + 1, endIdx);
            let valEndIdx = endIndex(str, endIdx, valStartIdx);
            const value = dec(str.slice(valStartIdx, valEndIdx));
            obj[key] = value;
        }
        index = endIdx + 1;
    }while (index < len)
    return obj;
}
function startIndex(str, index, max) {
    do {
        const code = str.charCodeAt(index);
        if (code !== 0x20 /*   */  && code !== 0x09 /* \t */ ) return index;
    }while (++index < max)
    return max;
}
function endIndex(str, index, min) {
    while(index > min){
        const code = str.charCodeAt(--index);
        if (code !== 0x20 /*   */  && code !== 0x09 /* \t */ ) return index + 1;
    }
    return min;
}
/**
 * Serialize data into a cookie header.
 *
 * Serialize a name value pair into a cookie string suitable for
 * http headers. An optional options object specifies cookie parameters.
 *
 * serialize('foo', 'bar', { httpOnly: true })
 *   => "foo=bar; httpOnly"
 */ function serialize(name, val, options) {
    const enc = options?.encode || encodeURIComponent;
    if (!cookieNameRegExp.test(name)) {
        throw new TypeError(`argument name is invalid: ${name}`);
    }
    const value = enc(val);
    if (!cookieValueRegExp.test(value)) {
        throw new TypeError(`argument val is invalid: ${val}`);
    }
    let str = name + "=" + value;
    if (!options) return str;
    if (options.maxAge !== undefined) {
        if (!Number.isInteger(options.maxAge)) {
            throw new TypeError(`option maxAge is invalid: ${options.maxAge}`);
        }
        str += "; Max-Age=" + options.maxAge;
    }
    if (options.domain) {
        if (!domainValueRegExp.test(options.domain)) {
            throw new TypeError(`option domain is invalid: ${options.domain}`);
        }
        str += "; Domain=" + options.domain;
    }
    if (options.path) {
        if (!pathValueRegExp.test(options.path)) {
            throw new TypeError(`option path is invalid: ${options.path}`);
        }
        str += "; Path=" + options.path;
    }
    if (options.expires) {
        if (!isDate(options.expires) || !Number.isFinite(options.expires.valueOf())) {
            throw new TypeError(`option expires is invalid: ${options.expires}`);
        }
        str += "; Expires=" + options.expires.toUTCString();
    }
    if (options.httpOnly) {
        str += "; HttpOnly";
    }
    if (options.secure) {
        str += "; Secure";
    }
    if (options.partitioned) {
        str += "; Partitioned";
    }
    if (options.priority) {
        const priority = typeof options.priority === "string" ? options.priority.toLowerCase() : undefined;
        switch(priority){
            case "low":
                str += "; Priority=Low";
                break;
            case "medium":
                str += "; Priority=Medium";
                break;
            case "high":
                str += "; Priority=High";
                break;
            default:
                throw new TypeError(`option priority is invalid: ${options.priority}`);
        }
    }
    if (options.sameSite) {
        const sameSite = typeof options.sameSite === "string" ? options.sameSite.toLowerCase() : options.sameSite;
        switch(sameSite){
            case true:
            case "strict":
                str += "; SameSite=Strict";
                break;
            case "lax":
                str += "; SameSite=Lax";
                break;
            case "none":
                str += "; SameSite=None";
                break;
            default:
                throw new TypeError(`option sameSite is invalid: ${options.sameSite}`);
        }
    }
    return str;
}
/**
 * URL-decode string value. Optimized to skip native call when no %.
 */ function decode(str) {
    if (str.indexOf("%") === -1) return str;
    try {
        return decodeURIComponent(str);
    } catch (e) {
        return str;
    }
}
/**
 * Determine if value is a Date.
 */ function isDate(val) {
    return __toString.call(val) === "[object Date]";
} //# sourceMappingURL=index.js.map
}),
"[project]/node_modules/@kinde/jwt-validator/dist/jwt-validator.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "validateToken",
    ()=>v
]);
const d = /* @__PURE__ */ new Map();
function b() {
    return ("TURBOPACK compile-time value", "undefined") < "u" ? window.crypto : globalThis.crypto;
}
async function A(e) {
    let t = 0;
    for(; t < 3;){
        const n = new AbortController(), a = setTimeout(()=>n.abort(), 5e3);
        try {
            const o = await fetch(`${e}/.well-known/jwks.json`, {
                signal: n.signal
            });
            if (clearTimeout(a), !o.ok) throw new Error(`Failed to fetch JWKS: ${o.status} ${o.statusText}`);
            return await o.json();
        } catch (o) {
            t++, console.error(`Attempt ${t} - Error fetching JWKS:`, o);
        }
    }
    throw new Error("Failed to fetch JWKS after multiple retries");
}
async function S(e, r, t) {
    t && d.delete(r);
    let n = d.get(r);
    n || (n = await A(r), d.set(r, n));
    try {
        return await E(e, JSON.stringify(n)), {
            valid: !0,
            message: "Token is valid"
        };
    } catch (a) {
        return t ? {
            valid: !1,
            message: a instanceof Error ? a.message : "Unknown Error"
        } : S(e, r, !0);
    }
}
async function E(e, r) {
    const [t, n, a] = e.split("."), k = JSON.parse(atob(t)).kid, i = J(r, k);
    if (!i || i.kty !== "RSA" || !i.n || !i.e || i.use !== "sig") throw new Error("Invalid JWK RSA key");
    const c = b();
    if (c != null && c.subtle) {
        const l = m(i.n), u = m(i.e), f = {
            name: "RSASSA-PKCS1-v1_5",
            hash: {
                name: "SHA-256"
            }
        };
        try {
            const s = {
                kty: "RSA",
                n: p(l),
                e: p(u),
                alg: "RS256"
            }, w = await c.subtle.importKey("jwk", s, f, !0, [
                "verify"
            ]), h = t + "." + n, g = j(a), y = await c.subtle.verify(f, w, g, new TextEncoder().encode(h));
            if (!y) throw new Error("Signature verification failed");
            return y;
        } catch (s) {
            throw s;
        }
    } else {
        const { KJUR: l, KEYUTIL: u, b64utoutf8: f } = await __turbopack_context__.A("[project]/node_modules/@kinde/jwt-validator/dist/jsrsasign-B2gZIJhY.js [app-rsc] (ecmascript, async loader)").then((g)=>g.j), s = JSON.parse(f(t));
        if (s.alg !== "RS256") throw new Error("Unsupported signature algorithm: " + s);
        const w = u.getKey(i);
        if (!l.jws.JWS.verifyJWT(e, w, {
            alg: [
                "RS256"
            ]
        })) throw new Error("Signature verification failed");
        return !0;
    }
}
function p(e) {
    let r = e.toString(16);
    r.length % 2 !== 0 && (r = "0" + r);
    const t = new Uint8Array(r.match(/.{1,2}/g).map((a)=>parseInt(a, 16)));
    return btoa(String.fromCharCode.apply(null, Array.from(t))).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
}
function J(e, r) {
    const t = JSON.parse(e);
    for (const n of t.keys)if (n.kid === r) return n;
    throw new Error(`JWK not found${r ? ` for kid ${r}` : ""}`);
}
function m(e) {
    e = e.replace(/[^A-Za-z0-9\-_]/g, ""), e += Array.from({
        length: (4 - e.length % 4) % 4
    }, ()=>"=").join("");
    const r = e.replace(/-/g, "+").replace(/_/g, "/");
    let t;
    try {
        t = atob(r);
    } catch (o) {
        throw new Error("Invalid Base64 string: " + o.message);
    }
    const n = Array.from(t, (o)=>o.charCodeAt(0).toString(16).padStart(2, "0")).join("");
    if (n.toLowerCase() === "ffffffffffffffff" || n.toLowerCase() === "7ff0000000000000") throw new Error("Decoded value represents NaN or Infinity");
    const a = n.replace(/^0+/, "");
    return BigInt("0x" + a || "0");
}
function j(e) {
    return Uint8Array.from(atob(e.replace(/-/g, "+").replace(/_/g, "/")), (r)=>r.charCodeAt(0));
}
const v = async (e)=>e.token ? e.domain ? e.token.split(".").length !== 3 ? {
        valid: !1,
        message: "Invalid JWT format"
    } : await S(e.token, e.domain) : {
        valid: !1,
        message: "Domain is required"
    } : {
        valid: !1,
        message: "Token is required"
    };
;
}),
"[project]/node_modules/uncrypto/dist/crypto.node.mjs [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "default",
    ()=>_crypto,
    "getRandomValues",
    ()=>getRandomValues,
    "randomUUID",
    ()=>randomUUID,
    "subtle",
    ()=>subtle
]);
var __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$crypto__$5b$external$5d$__$28$node$3a$crypto$2c$__cjs$29$__ = __turbopack_context__.i("[externals]/node:crypto [external] (node:crypto, cjs)");
;
const subtle = __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$crypto__$5b$external$5d$__$28$node$3a$crypto$2c$__cjs$29$__["default"].webcrypto?.subtle || {};
const randomUUID = ()=>{
    return __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$crypto__$5b$external$5d$__$28$node$3a$crypto$2c$__cjs$29$__["default"].randomUUID();
};
const getRandomValues = (array)=>{
    return __TURBOPACK__imported__module__$5b$externals$5d2f$node$3a$crypto__$5b$external$5d$__$28$node$3a$crypto$2c$__cjs$29$__["default"].webcrypto.getRandomValues(array);
};
const _crypto = {
    randomUUID,
    getRandomValues,
    subtle
};
;
}),
"[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "$",
    ()=>K,
    "A",
    ()=>Ne,
    "B",
    ()=>De,
    "C",
    ()=>Ve,
    "D",
    ()=>pe,
    "E",
    ()=>Ye,
    "F",
    ()=>ue,
    "G",
    ()=>We,
    "H",
    ()=>he,
    "I",
    ()=>je,
    "J",
    ()=>C,
    "K",
    ()=>Be,
    "L",
    ()=>y,
    "M",
    ()=>He,
    "N",
    ()=>qe,
    "O",
    ()=>Ge,
    "P",
    ()=>S,
    "Q",
    ()=>Je,
    "R",
    ()=>Ze,
    "S",
    ()=>W,
    "T",
    ()=>Le,
    "U",
    ()=>de,
    "V",
    ()=>ge,
    "W",
    ()=>ye,
    "X",
    ()=>ve,
    "Y",
    ()=>Ue,
    "Z",
    ()=>Re,
    "_",
    ()=>Me,
    "a",
    ()=>c,
    "a0",
    ()=>G,
    "a1",
    ()=>J,
    "a2",
    ()=>ee,
    "a3",
    ()=>R,
    "a4",
    ()=>_,
    "b",
    ()=>a,
    "c",
    ()=>H,
    "d",
    ()=>N,
    "e",
    ()=>Ie,
    "f",
    ()=>Pe,
    "g",
    ()=>M,
    "h",
    ()=>Ee,
    "i",
    ()=>be,
    "j",
    ()=>x,
    "k",
    ()=>Z,
    "l",
    ()=>D,
    "m",
    ()=>ne,
    "n",
    ()=>v,
    "o",
    ()=>Qe,
    "p",
    ()=>$e,
    "q",
    ()=>ae,
    "r",
    ()=>Xe,
    "s",
    ()=>V,
    "t",
    ()=>Q,
    "u",
    ()=>le,
    "v",
    ()=>j,
    "w",
    ()=>Y,
    "x",
    ()=>ze,
    "y",
    ()=>Fe,
    "z",
    ()=>g
]);
var K = /* @__PURE__ */ ((e)=>(e.email = "email", e.profile = "profile", e.openid = "openid", e.offline_access = "offline", e))(K || {}), G = /* @__PURE__ */ ((e)=>(e.none = "none", e.create = "create", e.login = "login", e))(G || {}), J = /* @__PURE__ */ ((e)=>(e.organizationDetails = "organization_details", e.organizationMembers = "organization_members", e.organizationPlanDetails = "organization_plan_details", e.organizationPaymentDetails = "organization_payment_details", e.organizationPlanSelection = "organization_plan_selection", e.paymentDetails = "payment_details", e.planSelection = "plan_selection", e.planDetails = "plan_details", e.profile = "profile", e))(J || {}), ee = /* @__PURE__ */ ((e)=>(e.organizationDetails = "organization_details", e.organizationMembers = "organization_members", e.organizationPlanDetails = "organization_plan_details", e.organizationPaymentDetails = "organization_payment_details", e.organizationPlanSelection = "organization_plan_selection", e.profile = "profile", e))(ee || {}), R = /* @__PURE__ */ ((e)=>(e.logout = "logout", e.login = "login", e.register = "registration", e.token = "token", e.profile = "profile", e))(R || {}), _ = /* @__PURE__ */ ((e)=>(e[e.refreshToken = 0] = "refreshToken", e[e.cookie = 1] = "cookie", e))(_ || {});
const H = (e)=>{
    const r = (i)=>btoa(i).replace(/\+/g, "-").replace(/\//g, "_").replace(/=+$/, "");
    if (e instanceof ArrayBuffer) {
        const i = new Uint8Array(e), s = String.fromCharCode(...i);
        return r(s);
    }
    const o = new TextEncoder().encode(e), n = String.fromCharCode(...o);
    return r(n);
}, M = (e = 28)=>{
    if (crypto) {
        const r = new Uint8Array(e / 2);
        return crypto.getRandomValues(r), Array.from(r, te).join("");
    } else return re(e);
};
function te(e) {
    return e.toString(16).padStart(2, "0");
}
function re(e = 28) {
    const r = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    let t = "";
    const o = r.length;
    for(let n = 0; n < e; n++)t += r.charAt(Math.floor(Math.random() * o));
    return t;
}
const Ie = (e)=>{
    e = e.split("?")[1];
    const r = new URLSearchParams(e);
    return {
        accessToken: r.get("access_token"),
        idToken: r.get("id_token"),
        expiresIn: +(r.get("expires_in") || 0)
    };
}, N = (e)=>e.replace(/\/$/, ""), ne = (e, r = !1)=>{
    const t = Array.isArray(e.audience) ? e.audience.join(" ") : e.audience || "", o = {
        login_hint: e.loginHint,
        is_create_org: e.isCreateOrg?.toString(),
        connection_id: e.connectionId,
        redirect_uri: e.redirectURL ? r ? e.redirectURL : N(e.redirectURL) : void 0,
        audience: t,
        scope: e.scope?.join(" ") || "email profile openid offline",
        prompt: e.prompt,
        lang: e.lang,
        org_code: e.orgCode,
        org_name: e.orgName,
        has_success_page: e.hasSuccessPage?.toString(),
        workflow_deployment_id: e.workflowDeploymentId,
        supports_reauth: e.supportsReauth?.toString(),
        plan_interest: e.planInterest,
        pricing_table_key: e.pricingTableKey,
        pages_mode: e.pagesMode
    };
    return Object.keys(o).forEach((n)=>o[n] === void 0 && delete o[n]), o;
}, L = (e)=>typeof e != "object" || e === null ? e : Array.isArray(e) ? e.map((r)=>L(r)) : Object.fromEntries(Object.entries(e).map(([r, t])=>[
            r.replace(/_([a-z])/g, (o, n)=>n.toUpperCase()),
            L(t)
        ])), oe = [
    // UTM tags
    "utm_source",
    "utm_medium",
    "utm_campaign",
    "utm_content",
    "utm_term",
    // Google Ads smart campaign tracking
    "gclid",
    "click_id",
    "hsa_acc",
    "hsa_cam",
    "hsa_grp",
    "hsa_ad",
    "hsa_src",
    "hsa_tgt",
    "hsa_kw",
    "hsa_mt",
    "hsa_net",
    "hsa_ver",
    // Marketing category
    "match_type",
    "keyword",
    "device",
    "ad_group_id",
    "campaign_id",
    "creative",
    "network",
    "ad_position",
    "fbclid",
    "li_fat_id",
    "msclkid",
    "twclid",
    "ttclid"
], Pe = async (e, r = R.login, t, o)=>{
    const n = `${e}/oauth2/auth`, i = S();
    if (t.reauthState) try {
        const f = L(JSON.parse(atob(t.reauthState)));
        t = {
            ...t,
            ...f
        }, delete t.reauthState;
    } catch (f) {
        const d = f instanceof Error ? f.message : "Unknown error";
        throw new Error(`Error handing reauth state: ${d}`);
    }
    if (!t.clientId) throw new Error("Error generating auth URL: Client ID missing");
    const s = {
        client_id: t.clientId,
        response_type: t.responseType || "code",
        ...ne(t, o?.disableUrlSanitization)
    };
    t.state || (t.state = M(32)), i && i.setSessionItem(c.state, t.state), s.state = t.state, t.nonce || (t.nonce = M(16)), s.nonce = t.nonce, i && i.setSessionItem(c.nonce, t.nonce);
    let l = "";
    if (t.codeChallenge) s.code_challenge = t.codeChallenge;
    else {
        const { codeVerifier: f, codeChallenge: d } = await se();
        l = f, i && i.setSessionItem(c.codeVerifier, f), s.code_challenge = d;
    }
    s.code_challenge_method = "S256", t.codeChallengeMethod && (s.code_challenge_method = t.codeChallengeMethod), !t.prompt && r === R.register && (s.prompt = G.create), t.properties && Object.keys(t.properties).forEach((f)=>{
        if (!oe.includes(f)) {
            console.warn("Unsupported Property for url generation: ", f);
            return;
        }
        const d = t.properties?.[f];
        d !== void 0 && (s[f] = d);
    });
    const u = new URLSearchParams(s).toString();
    return {
        url: new URL(`${n}?${u}`),
        state: s.state,
        nonce: s.nonce,
        codeChallenge: s.code_challenge,
        codeVerifier: l
    };
};
async function se() {
    const e = M(52), r = new TextEncoder().encode(e);
    let t = "";
    if (!crypto) t = H(btoa(e));
    else {
        const o = await crypto.subtle.digest("SHA-256", r);
        t = H(o);
    }
    return {
        codeVerifier: e,
        codeChallenge: t
    };
}
let $;
function Z(e, r) {
    if (D(), ("TURBOPACK compile-time value", "undefined") > "u") throw new Error("setRefreshTimer requires a browser environment");
    if (e <= 0) throw new Error("Timer duration must be positive");
    $ = window.setTimeout(r, Math.min(e * 1e3 - 1e4, 864e5));
}
function D() {
    $ !== void 0 && (window.clearTimeout($), $ = void 0);
}
const v = {
    framework: "",
    frameworkVersion: "",
    sdkVersion: ""
}, q = async ()=>{
    await S()?.removeItems(c.state, c.nonce, c.codeVerifier);
}, ae = ()=>`${v.framework}/${v.sdkVersion}/${v.frameworkVersion}/Javascript`, Ee = async ({ urlParams: e, domain: r, clientId: t, redirectURL: o, autoRefresh: n = !1, onRefresh: i })=>{
    const s = e.get("state"), l = e.get("code");
    if (!s || !l) return console.error("Invalid state or code"), {
        success: !1,
        error: "Invalid state or code"
    };
    const u = S();
    if (!u) return console.error("No active storage found"), {
        success: !1,
        error: "Authentication storage is not initialized"
    };
    (!v.framework || !v.frameworkVersion) && console.warn("Framework and version not set. Please set the framework and version in the config object");
    const f = await u.getSessionItem(c.state);
    if (s !== f) return console.error("Invalid state"), {
        success: !1,
        error: `Invalid state; supplied ${s}, expected ${f}`
    };
    const d = await u.getSessionItem(c.codeVerifier);
    if (d === null) return console.error("Code verifier not found"), {
        success: !1,
        error: "Code verifier not found"
    };
    const p = {
        "Content-type": "application/x-www-form-urlencoded; charset=UTF-8"
    };
    v.framework && (p["Kinde-SDK"] = ae());
    const I = {
        method: "POST",
        ...!a.useInsecureForRefreshToken && x(r) ? {
            credentials: "include"
        } : {},
        headers: new Headers(p),
        body: new URLSearchParams({
            client_id: t,
            code: l,
            code_verifier: d,
            grant_type: "authorization_code",
            redirect_uri: o
        })
    };
    let w;
    D();
    try {
        if (w = await fetch(`${r}/oauth2/token`, I), !w?.ok) {
            const k = await w.text();
            return console.error("Token exchange failed:", w.status, k), {
                success: !1,
                error: `Token exchange failed: ${w.status} - ${k}`
            };
        }
    } catch (k) {
        return q(), console.error("Token exchange failed:", k), {
            success: !1,
            error: `Token exchange failed: ${k}`
        };
    }
    const h = await w.json(), B = y();
    B && B.setItems({
        [c.accessToken]: h.access_token,
        [c.idToken]: h.id_token,
        [c.refreshToken]: h.refresh_token
    }), (a.useInsecureForRefreshToken || !x(r)) && u.setSessionItem(c.refreshToken, h.refresh_token), n && Z(h.expires_in, async ()=>{
        C({
            domain: r,
            clientId: t,
            onRefresh: i
        });
    }), q();
    const O = ((k)=>(k.search = "", k))(new URL(window.location.toString()));
    return window.history.replaceState(window.history.state, "", O), !h.access_token || !h.id_token || !h.refresh_token ? {
        success: !1,
        error: "No access token received"
    } : {
        success: !0,
        [c.accessToken]: h.access_token,
        [c.idToken]: h.id_token,
        [c.refreshToken]: h.refresh_token
    };
};
function ie(e) {
    const t = document.cookie.split("; ").find((o)=>o.startsWith(`${e}=`));
    if (!t) return null;
    try {
        const o = t.split("=")[1];
        return o ? decodeURIComponent(o) : null;
    } catch (o) {
        return console.error(`Error parsing cookie ${e}:`, o), null;
    }
}
const ce = "_kbrte", be = async ({ domain: e, clientId: r })=>{
    if (!e) return {
        success: !1,
        error: "Domain is required for authentication check"
    };
    if (!r) return {
        success: !1,
        error: "Client ID is required for authentication check"
    };
    const t = x(e), o = a.useInsecureForRefreshToken;
    let n = null;
    return t && !o && (n = ie(ce)), await C({
        domain: e,
        clientId: r,
        refreshType: n ? _.cookie : _.refreshToken
    });
}, x = (e)=>!e.match(/^(?:https?:\/\/)?[a-zA-Z0-9][.-a-zA-Z0-9]*\.kinde\.com$/i);
function V(e, r) {
    return r <= 0 ? [] : e.match(new RegExp(`.{1,${r}}`, "g")) || [];
}
var c = /* @__PURE__ */ ((e)=>(e.accessToken = "accessToken", e.idToken = "idToken", e.refreshToken = "refreshToken", e.state = "state", e.nonce = "nonce", e.codeVerifier = "codeVerifier", e))(c || {}), z = /* @__PURE__ */ ((e)=>(e.preWarning = "preWarning", e.timeout = "timeout", e))(z || {});
class W {
    async setItems(r) {
        await Promise.all(Object.entries(r).map(([t, o])=>this.setSessionItem(t, o)));
    }
    async removeItems(...r) {
        await Promise.all(r.map((t)=>this.removeSessionItem(t)));
    }
}
class Ue extends W {
    memCache = {};
    /**
   * Clears all items from session store.
   * @returns {void}
   */ async destroySession() {
        this.memCache = {};
    }
    /**
   * Sets the provided key-value store to the memory cache.
   * @param {string} itemKey
   * @param {unknown} itemValue
   * @returns {void}
   */ async setSessionItem(r, t) {
        if (await this.removeSessionItem(r), typeof t == "string") {
            V(t, a.maxLength).forEach((o, n)=>{
                this.memCache[`${a.keyPrefix}${r}${n}`] = o;
            });
            return;
        }
        this.memCache[`${a.keyPrefix}${String(r)}0`] = t;
    }
    /**
   * Gets the item for the provided key from the memory cache.
   * @param {string} itemKey
   * @returns {unknown | null}
   */ async getSessionItem(r) {
        if (this.memCache[`${a.keyPrefix}${String(r)}0`] === void 0) return null;
        let t = "", o = 0, n = `${a.keyPrefix}${String(r)}${o}`;
        for(; this.memCache[n] !== void 0;)t += this.memCache[n], o++, n = `${a.keyPrefix}${String(r)}${o}`;
        return t;
    }
    /**
   * Removes the item for the provided key from the memory cache.
   * @param {string} itemKey
   * @returns {void}
   */ async removeSessionItem(r) {
        for(const t in this.memCache)t.startsWith(`${a.keyPrefix}${String(r)}`) && delete this.memCache[t];
    }
}
function P(e) {
    return new Promise((r, t)=>{
        chrome.storage.local.get([
            e
        ], function(o) {
            chrome.runtime.lastError ? t(void 0) : r(o[e]);
        });
    });
}
class Re extends W {
    /**
   * Clears all items from session store.
   * @returns {void}
   */ async destroySession() {
        await chrome.storage.local.clear();
    }
    /**
   * Sets the provided key-value store to the chrome.store.local.
   * @param {string} itemKey
   * @param {unknown} itemValue
   * @returns {void}
   */ async setSessionItem(r, t) {
        if (await this.removeSessionItem(r), typeof t == "string") {
            V(t, a.maxLength).forEach(async (o, n)=>{
                await chrome.storage.local.set({
                    [`${a.keyPrefix}${r}${n}`]: o
                });
            });
            return;
        }
        await chrome.storage.local.set({
            [`${a.keyPrefix}${r}0`]: t
        });
    }
    /**
   * Gets the item for the provided key from the chrome.store.local cache.
   * @param {string} itemKey
   * @returns {unknown | null}
   */ async getSessionItem(r) {
        let t = "", o = 0, n = `${a.keyPrefix}${String(r)}${o}`;
        for(; await P(`${a.keyPrefix}${String(r)}${o}`) !== void 0;)t += await P(n), o++, n = `${a.keyPrefix}${String(r)}${o}`;
        return t;
    }
    /**
   * Removes the item for the provided key from the chrome.store.local cache.
   * @param {string} itemKey
   * @returns {void}
   */ async removeSessionItem(r) {
        let t = 0;
        for(; await P(`${a.keyPrefix}${String(r)}${t}`) !== void 0;)await chrome.storage.local.remove(`${a.keyPrefix}${String(r)}${t}`), t++;
    }
}
class Me extends W {
    constructor(){
        super(), a.useInsecureForRefreshToken && console.warn("LocalStorage store should not be used in production");
    }
    internalItems = /* @__PURE__ */ new Set();
    /**
   * Clears all items from session store.
   * @returns {void}
   */ async destroySession() {
        this.internalItems.forEach((r)=>{
            this.removeSessionItem(r);
        });
    }
    /**
   * Sets the provided key-value store to the localStorage cache.
   * @param {V} itemKey
   * @param {unknown} itemValue
   * @returns {void}
   */ async setSessionItem(r, t) {
        if (await this.removeSessionItem(r), this.internalItems.add(r), typeof t == "string") {
            V(t, a.maxLength).forEach((o, n)=>{
                localStorage.setItem(`${a.keyPrefix}${r}${n}`, o);
            });
            return;
        }
        localStorage.setItem(`${a.keyPrefix}${r}0`, t);
    }
    /**
   * Gets the item for the provided key from the localStorage cache.
   * @param {string} itemKey
   * @returns {unknown | null}
   */ async getSessionItem(r) {
        if (localStorage.getItem(`${a.keyPrefix}${r}0`) === null) return null;
        let t = "", o = 0, n = `${a.keyPrefix}${String(r)}${o}`;
        for(; localStorage.getItem(n) !== null;)t += localStorage.getItem(n), o++, n = `${a.keyPrefix}${String(r)}${o}`;
        return t;
    }
    /**
   * Removes the item for the provided key from the localStorage cache.
   * @param {V} itemKey
   * @returns {void}
   */ async removeSessionItem(r) {
        let t = 0;
        for(; localStorage.getItem(`${a.keyPrefix}${String(r)}${t}`) !== null;)localStorage.removeItem(`${a.keyPrefix}${String(r)}${t}`), t++;
        this.internalItems.delete(r);
    }
}
const a = {
    /**
   * The prefix to use for the storage keys.
   */ keyPrefix: "kinde-",
    /**
   * The maximum length of the storage.
   *
   * If the length is exceeded the items will be split into multiple storage items.
   */ maxLength: 2e3,
    /**
   * Use insecure storage for refresh token.
   *
   * Warning: This should only be used when you're not using a custom domain and no backend app to authenticate on.
   */ useInsecureForRefreshToken: !1,
    /**
   * The number of minutes of inactivity before tokens are considered expired.
   *
   * When undefined, activity tracking is disabled.
   */ activityTimeoutMinutes: void 0,
    /**
   * The number of minutes of inactivity before a pre-warning is shown.
   *
   * When undefined, pre-warning is disabled.
   */ activityTimeoutPreWarningMinutes: void 0,
    /**
   * The function to call when the activity timeout is reached.
   *
   * @param timeoutType - The type of timeout that occurred.
   */ onActivityTimeout: void 0
};
let E = null, b = null;
const le = ()=>{
    const e = y() ?? S();
    if (!e) throw new Error("Session manager not found");
    if (!a.activityTimeoutMinutes) throw new Error("No activity timeout minutes set");
    if (a.activityTimeoutPreWarningMinutes !== void 0 && a.activityTimeoutPreWarningMinutes >= a.activityTimeoutMinutes) throw new Error("activityTimeoutPreWarningMinutes must be less than activityTimeoutMinutes");
    E && clearTimeout(E), b && clearTimeout(b), b = setTimeout(async ()=>{
        try {
            await e.destroySession();
        } catch (t) {
            console.error("Failed to destroy secure session:", t);
        }
        const r = S();
        if (r && r !== e) try {
            await r.destroySession();
        } catch (t) {
            console.error("Failed to destroy insecure session:", t);
        }
        a.onActivityTimeout?.(z.timeout);
    }, a.activityTimeoutMinutes * 60 * 1e3), a.activityTimeoutPreWarningMinutes && (E = setTimeout(()=>{
        a.onActivityTimeout?.(z.preWarning);
    }, a.activityTimeoutPreWarningMinutes * 60 * 1e3));
}, Q = (e)=>{
    if (!e) throw new Error("Session manager not found");
    if (!a.activityTimeoutMinutes) return e;
    if (a.activityTimeoutPreWarningMinutes !== void 0 && a.activityTimeoutPreWarningMinutes >= a.activityTimeoutMinutes) throw new Error("activityTimeoutPreWarningMinutes must be less than activityTimeoutMinutes");
    const r = {
        get (t, o) {
            le();
            const n = t[o];
            return typeof n == "function" ? n.bind(t) : n;
        }
    };
    return new Proxy(e, r);
}, X = (e, r)=>{
    if (Array.isArray(e) && Array.isArray(r)) return Array.from(/* @__PURE__ */ new Set([
        ...e,
        ...r
    ]));
    if (e && typeof e == "object" && r && typeof r == "object") {
        const t = {
            ...e
        };
        for (const o of Object.keys(r))o in t ? t[o] = X(t[o], r[o]) : t[o] = r[o];
        return t;
    }
    return r;
};
async function F(e) {
    const r = y();
    if (!r) throw new Error("No active storage found.");
    const t = await r.getSessionItem(c.accessToken);
    if (!t) throw new Error("Authentication token not found.");
    const o = await j("iss");
    if (!o?.value) throw new Error("Domain (iss claim) not found.");
    let n;
    try {
        n = await fetch(`${o.value}/${e}`, {
            method: "GET",
            headers: {
                Authorization: `Bearer ${t}`,
                "Content-Type": "application/json"
            }
        });
    } catch (s) {
        throw new Error(`Failed to fetch from ${o.value}/${e}: ${s}`);
    }
    if (!n.ok) throw new Error(`API request failed with status ${n.status}`);
    return await n.json();
}
const T = async ({ url: e })=>{
    let r = [], t = await F(e);
    if (r = t.data, t.metadata?.has_more) {
        let o = t.metadata.next_page_starting_after;
        for(; t.metadata.has_more;)t = await F(`${e}?starting_after=${o}`), r = X(r, t.data), o = t.metadata.next_page_starting_after;
    }
    return r;
}, ue = async (e)=>{
    if (e?.forceApi) {
        const n = await T({
            url: "account_api/v1/permissions"
        });
        return {
            orgCode: n.org_code,
            permissions: n.permissions?.map((i)=>i.key) || []
        };
    }
    const r = await g();
    if (!r) return {
        orgCode: null,
        permissions: []
    };
    const t = r.permissions || r["x-hasura-permissions"] || [];
    return {
        orgCode: r.org_code || r["x-hasura-org-code"],
        permissions: t
    };
}, fe = (e)=>typeof e == "object" && e !== null && "permission" in e && "condition" in e, de = async (e)=>{
    if (!e || !e.permissions || e?.permissions?.length === 0) return !0;
    const { permissions: r } = e;
    let t;
    try {
        t = await ue({
            forceApi: e.forceApi
        });
    } catch (n) {
        return console.error("[hasPermissions] Error getting permissions", n), !1;
    }
    return (await Promise.all(r.map(async (n)=>fe(n) ? t.permissions.find((s)=>s === n.permission) ? await n.condition({
            permissionKey: n.permission,
            orgCode: t.orgCode
        }) : !1 : !!t.permissions.find((s)=>s === n)))).every((n)=>n === !0);
}, he = async (e)=>{
    const r = await j("roles");
    if (e?.forceApi || !r?.value) return (await T({
        url: "account_api/v1/roles"
    })).roles?.map((n)=>({
            id: n.id,
            name: n.name,
            key: n.key
        })) || [];
    const t = await g();
    return t ? !t.roles && !t["x-hasura-roles"] ? (console.warn("No roles found in token, ensure roles have been included in the token customisation within the application settings"), []) : t.roles || t["x-hasura-roles"] : [];
}, me = (e)=>typeof e == "object" && e !== null && "role" in e && "condition" in e, ge = async (e)=>{
    if (!e || !e.roles || e?.roles?.length === 0) return !0;
    const { roles: r } = e;
    let t;
    try {
        t = await he({
            forceApi: e.forceApi
        });
    } catch (n) {
        return console.error("[hasRoles] Error getting roles", n), !1;
    }
    return (await Promise.all(r.map(async (n)=>{
        if (me(n)) {
            const i = t.find((l)=>l.key === n.role);
            return i ? await n.condition(i) : !1;
        } else return t.map((s)=>s.key).includes(n);
    }))).every((n)=>n === !0);
}, we = async (e)=>{
    if (e?.forceApi) return (await T({
        url: "account_api/v1/feature_flags"
    })).feature_flags?.map((n)=>({
            key: n.key,
            value: n.value,
            type: n.type
        })) || [];
    const r = await g();
    if (!r) return null;
    const t = r.feature_flags || r["x-hasura-feature-flags"];
    return t ? Object.entries(t).map(([o, n])=>({
            key: o,
            value: n.v,
            type: n.t
        })) : null;
}, ke = (e)=>typeof e == "object" && e !== null && "flag" in e && "value" in e, ye = async (e)=>{
    if (!e || !e.featureFlags || e?.featureFlags?.length === 0) return !0;
    const { featureFlags: r } = e;
    let t;
    try {
        t = await we({
            forceApi: e.forceApi
        });
    } catch (n) {
        return console.error("[hasFeatureFlags] Error getting feature flags", n), !1;
    }
    return r.map((n)=>{
        if (ke(n)) {
            const i = t?.find((s)=>s.key === n.flag);
            return i !== void 0 && i.value === n.value;
        } else return t?.find((s)=>s.key === n) !== void 0;
    }).every((n)=>n === !0);
}, pe = async ()=>{
    const e = await T({
        url: "account_api/v1/entitlements"
    });
    return {
        orgCode: e.org_code,
        plans: e.plans?.map((r)=>({
                key: r.key,
                subscribedOn: r.subscribed_on
            })) || [],
        entitlements: e.entitlements?.map((r)=>({
                id: r.id,
                fixedCharge: r.fixed_charge,
                priceName: r.price_name,
                unitAmount: r.unit_amount,
                featureKey: r.feature_key,
                featureName: r.feature_name,
                entitlementLimitMax: r.entitlement_limit_max,
                entitlementLimitMin: r.entitlement_limit_min
            })) || []
    };
}, _e = (e)=>typeof e == "object" && e !== null && "entitlement" in e && "condition" in e, ve = async (e)=>{
    if (!e || !e.billingEntitlements || e?.billingEntitlements?.length === 0) return !0;
    const { billingEntitlements: r } = e;
    let t;
    try {
        t = await pe();
    } catch (n) {
        return console.error("[hasBillingEntitlements] Error getting entitlements", n), !1;
    }
    return (await Promise.all(r.map(async (n)=>{
        if (_e(n)) {
            const i = t.entitlements.find((s)=>s.priceName === n.entitlement);
            return i ? await n.condition(i) : !1;
        } else return t.entitlements.map((s)=>s.priceName).includes(n);
    }))).every((n)=>n === !0);
}, U = (e)=>e !== void 0 && typeof e == "object", Le = async (e)=>{
    const r = [];
    return e.roles && r.push(ge({
        roles: e.roles,
        forceApi: U(e.forceApi) ? e.forceApi.roles : e.forceApi
    })), e.permissions && r.push(de({
        permissions: e.permissions,
        forceApi: U(e.forceApi) ? e.forceApi.permissions : e.forceApi
    })), e.featureFlags && r.push(ye({
        featureFlags: e.featureFlags,
        forceApi: U(e.forceApi) ? e.forceApi.featureFlags : e.forceApi
    })), e.billingEntitlements && r.push(ve({
        billingEntitlements: e.billingEntitlements
    })), (await Promise.all(r)).every(Boolean);
};
function Se(e, r) {
    if (!e) return null;
    const t = e.split(".");
    if (t.length !== 3) return null;
    const o = t[1].replace(/-/g, "+").replace(/_/g, "/"), n = decodeURIComponent(atob(o).split("").map((i)=>"%" + ("00" + i.charCodeAt(0).toString(16)).slice(-2)).join(""));
    return JSON.parse(n);
}
const g = async (e = c.accessToken)=>{
    const r = y();
    if (!r) return null;
    const t = await r.getSessionItem(e === "accessToken" ? c.accessToken : c.idToken);
    if (!t) return null;
    const o = Se(t);
    return o || console.warn("No decoded token found"), o;
}, Y = async (e = "accessToken")=>g(e), j = async (e, r = "accessToken")=>{
    const t = await Y(r);
    return t ? {
        name: e,
        value: t[e]
    } : null;
}, ze = async ()=>{
    const e = await g();
    return e ? e.org_code || e["x-hasura-org-code"] : null;
}, Fe = async (e = c.accessToken)=>{
    const r = y();
    if (!r) return null;
    const t = await r.getSessionItem(e === "accessToken" ? c.accessToken : c.idToken);
    return t || null;
}, Ne = async (e, r)=>{
    if (r?.forceApi) {
        const s = (await T({
            url: "account_api/v1/feature_flags"
        })).feature_flags.find((l)=>l.name === e);
        return s ? s.value : null;
    }
    const t = await g();
    if (!t) return null;
    const o = t.feature_flags || t["x-hasura-feature-flags"];
    return o ? o[e]?.v ?? null : null;
}, De = async ()=>{
    const e = await Y("idToken");
    if (!e) return null;
    const { sub: r } = e;
    return r ? {
        id: e.sub,
        givenName: e.given_name,
        familyName: e.family_name,
        email: e.email,
        picture: e.picture
    } : (console.error("No sub in idToken"), null);
}, Ve = async (e, r)=>{
    if (r?.forceApi) return F(`account_api/v1/permission/${encodeURIComponent(e)}`);
    const t = await g();
    if (!t) return {
        permissionKey: e,
        orgCode: null,
        isGranted: !1
    };
    const o = t.permissions || [];
    return {
        permissionKey: e,
        orgCode: t.org_code,
        isGranted: !!o.includes(e)
    };
}, We = async ()=>{
    const e = await g("idToken");
    return e ? !e.org_codes && !e["x-hasura-org-codes"] ? (console.warn("Org codes not found in token, ensure org codes have been included in the token customisation within the application settings"), null) : e.org_codes || e["x-hasura-org-codes"] : null;
}, je = async (e)=>{
    try {
        const r = await g("accessToken");
        if (!r) return !1;
        if (!r.exp) return console.error("Token does not have an expiry"), !1;
        const t = r.exp < Math.floor(Date.now() / 1e3);
        return t && e?.useRefreshToken ? (await C({
            domain: e.domain,
            clientId: e.clientId
        })).success : !t;
    } catch (r) {
        return console.error("Error checking authentication:", r), !1;
    }
}, C = async ({ domain: e, clientId: r, refreshType: t = _.refreshToken, onRefresh: o })=>{
    const n = (l)=>(o && o(l), l);
    if (!e) return n({
        success: !1,
        error: "Domain is required for token refresh"
    });
    if (!r) return n({
        success: !1,
        error: "Client ID is required for token refresh"
    });
    let i = "", s;
    if (a.useInsecureForRefreshToken || !x(e) ? s = S() : s = y(), t === _.refreshToken) {
        if (!s) return n({
            success: !1,
            error: "No active storage found"
        });
        if (i = await s.getSessionItem(c.refreshToken), !i) return n({
            success: !1,
            error: "No refresh token found"
        });
    }
    D();
    try {
        const l = await fetch(`${N(e)}/oauth2/token`, {
            method: "POST",
            ...t === _.cookie && {
                credentials: "include"
            },
            headers: {
                "Content-type": "application/x-www-form-urlencoded; charset=UTF-8"
            },
            body: new URLSearchParams({
                ...t === _.refreshToken && {
                    refresh_token: i
                },
                grant_type: "refresh_token",
                client_id: r
            }).toString()
        });
        if (!l.ok) return n({
            success: !1,
            error: "Failed to refresh token"
        });
        const u = await l.json();
        if (u.access_token) {
            const f = y();
            return f ? (Z(u.expires_in, async ()=>{
                C({
                    domain: e,
                    clientId: r,
                    refreshType: t,
                    onRefresh: o
                });
            }), s && (await f.setSessionItem(c.accessToken, u.access_token), u.id_token && await f.setSessionItem(c.idToken, u.id_token), u.refresh_token && await s.setSessionItem(c.refreshToken, u.refresh_token)), n({
                success: !0,
                [c.accessToken]: u.access_token,
                [c.idToken]: u.id_token,
                [c.refreshToken]: u.refresh_token
            })) : n({
                success: !1,
                error: "No active storage found"
            });
        }
    } catch (l) {
        return n({
            success: !1,
            error: `No access token received: ${l}`
        });
    }
    return n({
        success: !1,
        error: "No access token received"
    });
}, m = {
    secure: null,
    insecure: null
}, Be = (e)=>{
    if (a.activityTimeoutMinutes) {
        m.secure = Q(e);
        return;
    }
    m.secure = e;
}, y = ()=>m.secure || null, He = ()=>m.secure !== null, qe = ()=>{
    m.secure = null;
}, Ge = (e)=>{
    if (a.activityTimeoutMinutes) {
        m.insecure = Q(e);
        return;
    }
    m.insecure = e;
}, S = ()=>m.insecure || m.secure || null, Je = ()=>m.insecure !== null, Ze = ()=>{
    m.insecure = null;
}, Qe = async (e)=>(console.warn("Warning: generateProfileUrl is deprecated. Please use generatePortalUrl instead."), $e({
        domain: e.domain,
        returnUrl: e.returnUrl,
        subNav: e.subNav
    }));
function Te(e, r = []) {
    try {
        const t = new URL(e);
        return !r.includes(t.protocol) && !!t.host;
    } catch  {
        return !1;
    }
}
const $e = async ({ domain: e, returnUrl: r, subNav: t })=>{
    const o = y();
    if (!o) throw new Error("generatePortalUrl: Active storage not found");
    const n = await o.getSessionItem(c.accessToken);
    if (!n) throw new Error("generatePortalUrl: Access Token not found");
    if (!e || typeof e != "string") {
        const u = await j("iss");
        if (!u?.value || typeof u.value != "string") throw new Error("generatePortalUrl: Unable to determine domain from access token");
        e = u.value;
    }
    if (!Te(r, [
        "ftp:",
        "ws:"
    ])) throw new Error("generatePortalUrl: returnUrl must be an absolute URL");
    const i = new URLSearchParams({
        subnav: t || J.profile,
        return_url: r
    }), s = await fetch(`${N(e)}/account_api/v1/portal_link?${i.toString()}`, {
        headers: {
            Authorization: `Bearer ${n}`
        }
    });
    if (!s.ok) throw new Error(`Failed to fetch profile URL: ${s.status} ${s.statusText}`);
    const l = await s.json();
    if (!l.url || typeof l.url != "string") throw new Error("Invalid URL received from API");
    try {
        return {
            url: new URL(l.url)
        };
    } catch (u) {
        throw console.error(u), new Error(`Invalid URL format received from API: ${l.url}`);
    }
}, xe = ()=>window.self !== window.top, Xe = async ({ url: e, popupOptions: r = {}, handleResult: t, forcePopup: o = !1 })=>{
    xe() || o ? await Ce({
        url: e,
        popupOptions: r,
        handleResult: t
    }) : document.location = e;
}, Ce = async ({ url: e, popupOptions: r = {}, handleResult: t })=>{
    const { width: o = 500, height: n = 600, left: i = (window.screen.width - o) / 2, top: s = (window.screen.height - n) / 2 } = r, l = window.open(e, "kinde_auth_popup", `width=${o},height=${n},left=${i},top=${s},scrollbars=yes,resizable=yes,toolbar=no,menubar=no,location=no,status=no`);
    if (!l) throw new Error("Popup was blocked by the browser");
    const u = ()=>new Promise((f)=>{
            const d = (p)=>{
                if (p.origin === window.location.origin && p.data && p.data.type === "KINDE_AUTH_RESULT") {
                    window.removeEventListener("message", d);
                    const A = new URLSearchParams();
                    Object.entries(p.data.result).forEach(([I, w])=>{
                        A.append(I, w);
                    }), t?.(A).then(()=>f());
                }
            };
            window.addEventListener("message", d);
        });
    try {
        await u();
    } catch (f) {
        throw new Error("Popup authentication failed: " + f);
    }
    return l;
}, Ye = {
    __esModule: !0,
    default: async ()=>(await import(/* webpackIgnore: true */ "./expoSecureStore-BTi2p58Y.js")).ExpoSecureStore
};
;
}),
"[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript) <export p as generatePortalUrl>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "generatePortalUrl",
    ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["p"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript)");
}),
"[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript) <export Y as MemoryStorage>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "MemoryStorage",
    ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["Y"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript)");
}),
"[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript) <export K as setActiveStorage>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "setActiveStorage",
    ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["K"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript)");
}),
"[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript) <export a as StorageKeys>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "StorageKeys",
    ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["a"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f40$kinde$2d$oss$2f$kinde$2d$typescript$2d$sdk$2f$node_modules$2f40$kinde$2f$js$2d$utils$2f$dist$2f$main$2d2d$ltuV3nd$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/@kinde-oss/kinde-typescript-sdk/node_modules/@kinde/js-utils/dist/main--ltuV3nd.js [app-rsc] (ecmascript)");
}),
"[project]/node_modules/lucide-react/dist/esm/shared/src/utils.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @license lucide-react v0.553.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ __turbopack_context__.s([
    "hasA11yProp",
    ()=>hasA11yProp,
    "mergeClasses",
    ()=>mergeClasses,
    "toCamelCase",
    ()=>toCamelCase,
    "toKebabCase",
    ()=>toKebabCase,
    "toPascalCase",
    ()=>toPascalCase
]);
const toKebabCase = (string)=>string.replace(/([a-z0-9])([A-Z])/g, "$1-$2").toLowerCase();
const toCamelCase = (string)=>string.replace(/^([A-Z])|[\s-_]+(\w)/g, (match, p1, p2)=>p2 ? p2.toUpperCase() : p1.toLowerCase());
const toPascalCase = (string)=>{
    const camelCase = toCamelCase(string);
    return camelCase.charAt(0).toUpperCase() + camelCase.slice(1);
};
const mergeClasses = (...classes)=>classes.filter((className, index, array)=>{
        return Boolean(className) && className.trim() !== "" && array.indexOf(className) === index;
    }).join(" ").trim();
const hasA11yProp = (props)=>{
    for(const prop in props){
        if (prop.startsWith("aria-") || prop === "role" || prop === "title") {
            return true;
        }
    }
};
;
 //# sourceMappingURL=utils.js.map
}),
"[project]/node_modules/lucide-react/dist/esm/defaultAttributes.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @license lucide-react v0.553.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ __turbopack_context__.s([
    "default",
    ()=>defaultAttributes
]);
var defaultAttributes = {
    xmlns: "http://www.w3.org/2000/svg",
    width: 24,
    height: 24,
    viewBox: "0 0 24 24",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: 2,
    strokeLinecap: "round",
    strokeLinejoin: "round"
};
;
 //# sourceMappingURL=defaultAttributes.js.map
}),
"[project]/node_modules/lucide-react/dist/esm/Icon.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @license lucide-react v0.553.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ __turbopack_context__.s([
    "default",
    ()=>Icon
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$defaultAttributes$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/lucide-react/dist/esm/defaultAttributes.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/lucide-react/dist/esm/shared/src/utils.js [app-rsc] (ecmascript)");
;
;
;
const Icon = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["forwardRef"])(({ color = "currentColor", size = 24, strokeWidth = 2, absoluteStrokeWidth, className = "", children, iconNode, ...rest }, ref)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["createElement"])("svg", {
        ref,
        ...__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$defaultAttributes$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"],
        width: size,
        height: size,
        stroke: color,
        strokeWidth: absoluteStrokeWidth ? Number(strokeWidth) * 24 / Number(size) : strokeWidth,
        className: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["mergeClasses"])("lucide", className),
        ...!children && !(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["hasA11yProp"])(rest) && {
            "aria-hidden": "true"
        },
        ...rest
    }, [
        ...iconNode.map(([tag, attrs])=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["createElement"])(tag, attrs)),
        ...Array.isArray(children) ? children : [
            children
        ]
    ]));
;
 //# sourceMappingURL=Icon.js.map
}),
"[project]/node_modules/lucide-react/dist/esm/createLucideIcon.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @license lucide-react v0.553.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ __turbopack_context__.s([
    "default",
    ()=>createLucideIcon
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/next/dist/server/route-modules/app-page/vendored/rsc/react.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/lucide-react/dist/esm/shared/src/utils.js [app-rsc] (ecmascript)");
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$Icon$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/lucide-react/dist/esm/Icon.js [app-rsc] (ecmascript)");
;
;
;
const createLucideIcon = (iconName, iconNode)=>{
    const Component = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["forwardRef"])(({ className, ...props }, ref)=>(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$next$2f$dist$2f$server$2f$route$2d$modules$2f$app$2d$page$2f$vendored$2f$rsc$2f$react$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["createElement"])(__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$Icon$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"], {
            ref,
            iconNode,
            className: (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["mergeClasses"])(`lucide-${(0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["toKebabCase"])((0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["toPascalCase"])(iconName))}`, `lucide-${iconName}`, className),
            ...props
        }));
    Component.displayName = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$shared$2f$src$2f$utils$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["toPascalCase"])(iconName);
    return Component;
};
;
 //# sourceMappingURL=createLucideIcon.js.map
}),
"[project]/node_modules/lucide-react/dist/esm/icons/arrow-right.js [app-rsc] (ecmascript)", ((__turbopack_context__) => {
"use strict";

/**
 * @license lucide-react v0.553.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */ __turbopack_context__.s([
    "__iconNode",
    ()=>__iconNode,
    "default",
    ()=>ArrowRight
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$createLucideIcon$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/lucide-react/dist/esm/createLucideIcon.js [app-rsc] (ecmascript)");
;
const __iconNode = [
    [
        "path",
        {
            d: "M5 12h14",
            key: "1ays0h"
        }
    ],
    [
        "path",
        {
            d: "m12 5 7 7-7 7",
            key: "xquz4c"
        }
    ]
];
const ArrowRight = (0, __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$createLucideIcon$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"])("arrow-right", __iconNode);
;
 //# sourceMappingURL=arrow-right.js.map
}),
"[project]/node_modules/lucide-react/dist/esm/icons/arrow-right.js [app-rsc] (ecmascript) <export default as ArrowRight>", ((__turbopack_context__) => {
"use strict";

__turbopack_context__.s([
    "ArrowRight",
    ()=>__TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$icons$2f$arrow$2d$right$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__["default"]
]);
var __TURBOPACK__imported__module__$5b$project$5d2f$node_modules$2f$lucide$2d$react$2f$dist$2f$esm$2f$icons$2f$arrow$2d$right$2e$js__$5b$app$2d$rsc$5d$__$28$ecmascript$29$__ = __turbopack_context__.i("[project]/node_modules/lucide-react/dist/esm/icons/arrow-right.js [app-rsc] (ecmascript)");
}),
];

//# sourceMappingURL=node_modules_06bbf63a._.js.map